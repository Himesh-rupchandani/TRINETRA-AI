# ---------------------------------------------------------------
# DEMO MODE (npm run dev:demo) — synthetic data, no backend needed.
# The whole product works offline: simulated detections, demo route,
# clearly-watermarked synthetic evidence. Never used in live mode.
# ---------------------------------------------------------------
VITE_USE_MOCKS=true
VITE_REALTIME_TRANSPORT=off
VITE_LIVE_STREAMS=true
