fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
sqlalchemy>=2.0.28
alembic>=1.13.1
python-dotenv>=1.0.1
python-multipart>=0.0.9
opencv-python-headless>=4.9.0  # headless: no libGL/X11 required on servers
numpy>=1.26.0
pillow>=10.2.0
httpx>=0.27.0
# ---- Vehicle detection: two interchangeable backends ----
# RF-DETR (default when installed) — Roboflow's real-time DETR, SOTA on COCO.
#   pip install rfdetr            (pulls torch + torchvision)
#   CPU-only torch: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   Set DETECTOR_BACKEND=rfdetr|yolo|auto to choose; without rfdetr the app
#   falls back to the YOLO11 backend below, so this one is optional.
rfdetr>=1.0.0
#
# YOLO11 (previous default, still fully supported fallback).
# NOTE: ultralytics pulls the GUI opencv-python wheel; on headless servers
# re-run `pip install --force-reinstall --no-deps opencv-python-headless` after.
ultralytics>=8.3.0
# ANPR/OCR for uploaded CCTV videos (bundled ONNX models — works fully offline).
# The pipeline degrades gracefully to "Unknown" plates when this is absent.
rapidocr-onnxruntime>=1.0.0
pytest>=8.1.0
pytest-asyncio>=0.23.0
