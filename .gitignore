.venv/
__pycache__/
*.pyc

# Workspace snapshot archives (stale code copies — never commit)
workspace-*.zip

# Rendered analysis videos (large, fully regenerable from the pipeline)
outputs/**/annotated_video.mp4
outputs/**/annotated_raw.mp4
