# model weights & dev artifacts — never commit
*.pt
*.onnx
*.engine
models_dev/
# Runtime evidence OUTPUT (JPEG frames/crops written by the pipeline) —
# the evidence/ python package source itself IS committed.
evidence/**/*.jpg
__pycache__/
*.pyc
