# model weights & dev artifacts — never commit
*.pt
*.onnx
*.engine
models_dev/
# CV evidence OUTPUT (runtime frames) — the evidence/ *source package* is tracked.
/evidence/*/
evidence_out/
__pycache__/
*.pyc
