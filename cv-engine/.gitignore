# model weights & dev artifacts — never commit
*.pt
*.onnx
*.engine
models_dev/
# generated evidence artifacts only — "evidence/" itself is a SOURCE package
evidence_out/
__pycache__/
*.pyc

# local demo feed videos (large binaries — place manually, see scripts/run_feed_demo.py)
feeds/
