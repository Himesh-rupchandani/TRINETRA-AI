[pytest]
testpaths = tests
addopts = -ra --timeout=60 --timeout-method=thread
markers =
    integration: needs a live Sentinel feed, real model weights or a running backend
    slow: takes more than a few seconds
filterwarnings =
    ignore::DeprecationWarning
