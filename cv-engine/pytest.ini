[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    integration: end-to-end runs needing model weights + a sample clip (not run by default)
    live: tests that hit the real Sentinel Government feed (need TRINETRA_LIVE=1)
addopts = -m "not live and not integration"
